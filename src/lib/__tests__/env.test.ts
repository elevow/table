// Legacy file replaced by env.browser.test.ts and env.node.test.ts
// Keep a skipped placeholder test to satisfy Jest's requirement.
describe('env legacy placeholder', () => {
	it.skip('replaced by env.browser.test.ts and env.node.test.ts', () => {
		expect(true).toBe(true);
	});
});
